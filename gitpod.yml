tasks:
  - name: CMS
    init: npm install && npm run build && gp sync-done install
    command: npm run dev
  - name: Docs
    init: gp sync-await install
    command: npm run docs:dev

ports:
  - port: 8080
    onOpen: open-preview

vscode:
  extensions:
    - octref.vetur@0.23.0:TEzauMObB6f3i2JqlvrOpA== 
